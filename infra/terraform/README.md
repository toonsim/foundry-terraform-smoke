# Terraform

This Terraform root is generated by Project Foundry as a provider-free
infrastructure workspace.

Provider-specific capabilities may add providers, backends, modules, resources,
variables, and outputs. Keep state files and provider caches out of Git.
